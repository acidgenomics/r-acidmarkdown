# R session information {.tabset}

```{r session_info}
markdownHeader("YAML params", level = 2)
print(params)

markdownHeader("`_setup.R`", level = 2)
writeLines(import("_setup.R", format = "lines"))

markdownHeader("`sessionInfo()`", level = 2)
utils::sessionInfo()
```
